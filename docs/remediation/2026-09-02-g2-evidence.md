# Generate Test Cases V1 Remediation — G2 Evidence

Date: 2026-09-02 (Asia/Shanghai)

## Outcome

G2 / Task 14 is engineering-complete at commit `da539386a15a91eb3483fe11a7505a62dd178814`. The gate now retains ten hand-reviewed end-to-end journeys, a closed fourteen-reversal hard-gate manifest, explicit metamorphic dependency-closure checks, fourteen durable-recovery crash layouts plus an independent baseline, JSON and Markdown goldens, and a 100-run installed-shape determinism ledger. This gate does not establish benchmark eligibility and does not authorize installation or an RC tag.

## Candidate

- Commit: `da539386a15a91eb3483fe11a7505a62dd178814` — `test: close task 14 recovery gates`
- Parent: `8e254e6cfbe1af46413116cc34f7497b7dfa07b5`
- Change size: 13 files, 445 insertions, 28 deletions.
- Production source changes in G2: none; Task 14 changed tests and reviewed fixtures only.
- Post-commit worktree before this evidence record: clean.
- Environment: Node `v24.18.0`; npm `11.16.0`.

## RED to GREEN

The pre-change suite was green, but the Task 14 audit found that it did not test the required states. Those absences were treated as missing regressions, not as proof of correct behavior:

| Audit RED / review finding | Regression added or tightened | GREEN evidence |
| --- | --- | --- |
| No true E1 → unknown/E0 → Blocked dependency-closure metamorphic | Two legal four-artifact inputs compare an E1 Oracle (Conditional) with an absent Oracle plus typed blocker (Blocked); the independent shipping Case remains Grounded with the same ID, lane, content, and formal total | `metamorphic: E1→E0 blocks only the dependent module` passes |
| Empty `required_oracle_refs` could be confused with E0 | Kept a separate remediation regression proving a Case-supplied supported Oracle may still close an obligation | `empty Oracle prebinding preserves a Case-supplied Oracle` passes |
| `accepted-before-checkpoint` and `case-drafts-before-bundle` represented the same practical layout | Built each state through real runner stage progression and removed only the write that defines the crash window | Every recovery fixture test passes and reaches an independent baseline byte-for-byte |
| No distinct JSON-only output crash; no missing/mismatched bundle behind a finished checkpoint | Added `json-bundle-before-markdown`, `finished-checkpoint-bundle-missing`, and `finished-checkpoint-bundle-digest-mismatch` | All three recover deterministically without accepting stale output |
| Hard-gate inventory could silently gain unused rows | Loader now requires exactly the fourteen behavior-tested reversals | Closed manifest loads and all fourteen behavioral assertions pass |
| First recovery layouts were synthesized by deleting from a completed run | Reworked them to use real source → evidence → behavior → case stage promotion and deliberately retained the earlier checkpoint at the intended boundary | Focused recovery review passed 3/3 |
| Exact layout assertions initially omitted the required empty `staging/` directory | Preserved and explicitly asserted the empty staging directory in the three critical crash windows | Final specification and quality reviews approved with no findings |

An attempted direct construction of an impossible Agent-authored E0 evidence claim was rejected during review and withdrawn. The final regression uses only Schema-valid public artifacts and does not weaken evidence gates.

## Frozen Task 14 assets

### Ten journeys

The exact reviewed journey set is:

1. `all-e3`
2. `e1-conditional`
3. `partial-blocked`
4. `risk-only-exploratory`
5. `all-blocked`
6. `all-not-applicable`
7. `local-source-conflict`
8. `multi-module-interaction`
9. `clarification-conditional`
10. `clarification-grounded`

The test checks every output lane, formal/not-applicable accounting, delivery status, both clarification outcomes, the actual installed runner reply sequence, a forbidden public `record_only` selector, and stale-checkpoint rejection. JSON and Markdown bytes for all ten scenarios are compared with committed, hand-owned goldens.

### Metamorphic coverage

The gate explicitly checks these required transformations:

- artifact set-like reorder preserves the canonical bundle;
- adding an independent module preserves previous IDs and lanes;
- E3 → E1 changes only Grounded → Conditional;
- E1 → unknown/E0 changes only the dependent module to Blocked;
- E1 support-review rejection blocks only its dependency closure;
- provided → unknown capability blocks only the affected Case;
- semantic no-op revisions preserve stable IDs;
- a scoped final Decision changes only its dependency closure.

Adjacent hard regressions additionally protect Case-supplied optional Oracle prebinding, approved-assumption classification, Exploratory accounting, interaction completeness, E2 target/cycle validity, missing formal Oracle retention, and the non-public `record_only` policy.

### Fourteen reversal gates

The closed manifest contains exactly:

`E1-as-Grounded`, `E0-as-Conditional`, `unsupported-support-review`, `approved-assumption-as-Grounded`, `Exploratory-in-denominator`, `NotApplicable-in-numerator`, `missing-Blocked-Test-Point`, `skipped-interaction-cell`, `invalid-E2-target`, `E2-cycle`, `revision-in-stable-ID`, `old-checkpoint-winning`, `record_only-lowering-gates`, and `record_only-public-selector`.

### Fifteen recovery layouts

The deterministic fixture set is:

`staging-before-promotion`, `accepted-before-checkpoint`, `obligations-before-checkpoint`, `case-drafts-before-bundle`, `bundle-before-finished-checkpoint`, `json-bundle-before-markdown`, `finished-checkpoint-before-current`, `truncated-checkpoint`, `old-current-pointer`, `r000-finished-r001-source`, `r001-partially-accepted`, `r000-finished-r001-reopen`, `finished-checkpoint-bundle-missing`, `finished-checkpoint-bundle-digest-mismatch`, and `grounded-revision`.

The first fourteen are enumerated crash/recovery boundaries; `grounded-revision` is the independent accepted baseline. Critical layouts are asserted recursively, including the empty `staging/` directory. Recovered bundle and Markdown bytes, digests, checkpoint, and `current.json` binding must match the independent baseline.

## Verification

Focused controller run:

```text
node --test --test-concurrency=1 \
  test/core/metamorphic.test.mjs \
  test/golden/canonical-bundles.test.mjs \
  test/interface/full-journey.test.mjs \
  test/recovery/checkpoint-recovery.test.mjs
```

Exit 0: 76/76 PASS in 89.8 s.

Fresh engineering check:

```text
npm run check
```

Exit 0:

- TypeScript no-emit: PASS;
- generated build `--check`: PASS;
- primary suite: 829/829 PASS in 316.403 s;
- repeatability: 1/1 PASS, 100 fresh installed-shape runs and 400 real CLI calls in 46.456 s.

Exact required G2 command:

```text
npm test -- --test-name-pattern='journey|golden|metamorphic|determinism'
```

Exit 0:

- primary suite: 829/829 PASS in 315.389 s;
- filtered repeatability: 1/1 PASS in 46.495 s.

The repository `npm test` script is `node --test --test-concurrency=1 test/**/*.test.mjs && node --test test/repeatability/*.repeat.mjs`; therefore npm appends the supplied name filter only to the second command. The first command deliberately ran the complete primary suite. This command behavior is recorded rather than misreported as a narrow-only run.

Independent final reviews:

- specification review: APPROVED; no Critical, Important, or Minor findings;
- code-quality review: APPROVED; no Critical, Important, or Minor findings;
- focused review rerun: 3/3 PASS; TypeScript and `git diff --check` PASS;
- reviewers made no file changes.

## Determinism ledger

The post-commit extraction invoked `runInstalledAcceptedRepetitions('all-e3', 100)` at `da539386a15a91eb3483fe11a7505a62dd178814`.

| Runs | Bundle SHA-256 | Markdown SHA-256 | Reply sequence |
| --- | --- | --- | --- |
| 001–100 (100/100 identical) | `49bbd4b0effd2295c5ff8ca95c2412fded77f5e6537eb7baee4e178c43babdda` | `b556100ec5995c75d858aaf2dff2f7c254829b47d710ba4ff5d5ccafb928722f` | `need_artifact/evidence_claims` → `need_artifact/behavior_views` → `need_artifact/case_drafts` → `finished/done` |

Runner bytes used by all 100 runs: `5725952e95195ba85f9887c46f4b65bdfe5035fa80b27a85ffa0c7f71921db16`.

## Digests at G2 closure

- modular source-tree aggregate: `74f9a7a5ae913eb98e32161c387093e5cb9c5e93ecf456024de5903ab45c271d`
- generated `test-compiler.mjs`: `5725952e95195ba85f9887c46f4b65bdfe5035fa80b27a85ffa0c7f71921db16`
- Schema aggregate: `f02e2a012f0f06f05256e5b0fbc4988fceccf3322aae82b70b00fac403e2f4a6`
- `schema-manifest.json`: `f706ff91b97ea1ee336f5abda96d3ff348804c71a5961db217bdddd971bb047a`
- `SKILL.md`: `06f974e581b710490cdb45a1cfc79e3014ff4326579b56618d2edcad87999a94`
- journey JSON golden aggregate: `6e1b8278f17a6bd4deb23ae2f6aa4f97b5e493b3419b69dde993c4afbd7ed106`
- journey Markdown golden aggregate: `d5549a463bf59ad1d677d51b4ce47f5c6d4e713dbf0c8a021ba1f5cc9ee8f159`
- recovery fixture aggregate: `c9e4a7e0d5e691e816312ba9604bcd0cbcac1036b6f30a63fb0ce9a5c434c254`
- Task 14 test aggregate: `1c2449ece8743925aca330406b668f957052760fe41637864e167bf0e79df847`

These are G2 engineering digests, not the frozen G4/G5 release candidate. Any later production, Schema, Skill, benchmark, or bundle change invalidates release evidence. No installation, push, merge, or RC tag occurred in G2.
