# Generate Test Cases V1 Remediation — G4 Pre-freeze Evidence

Date: 2026-09-02 (Asia/Shanghai)

## Scope and evidence status

This record closes the defects found by G4 forward testing and records the
pre-freeze engineering evidence. It does not claim release readiness. All clean
checkout, validator, forward-test, and benchmark observations made before the
final candidate is frozen are explicitly preliminary; the final G4 evidence is
rerun from an isolated checkout of one immutable candidate SHA.

The retained benchmark is still synthetic and incomplete. The only valid release
status is `release_blocked_by_benchmark_insufficient_evidence`. Installation,
overwrite, an RC tag, and a release are forbidden.

## Candidate history and environment

- G4 input commit: `be230d55d62c428f4bffefa42c65c44f981e4d8e`.
- NotApplicable Adapter fix: `adf6d735563c549379f796048c43fc22e5d4b5da`.
- blocker evidence-closure fix: `c76fcee9ef984642226d9fdbd63a217c6de93439`.
- Branch: `codex/generate-test-cases-remediation`.
- Node: `v24.18.0`; npm: `11.16.0`.
- Validator Python: `/private/tmp/generate-test-cases-validator-py/bin/python`
  (`Python 3.9.6`, PyYAML `6.0.2`).
- Validator: `/Users/zhangxudong/.codex/skills/.system/skill-creator/scripts/quick_validate.py`
  (SHA-256 `1fd66498c219616fd9249eacdf16c458412ea9065a9d887fd716aeef03907762`).

## RED to GREEN

| Forward or gate RED | Fix boundary | GREEN evidence |
| --- | --- | --- |
| A complete-PRD agent used a generic same-scope claim as independent evidence for NotApplicable | `references/behavior-views.md` now requires a semantic review independent of the excluded responsibility, direct proof of exclusion, and Blocked when that proof is absent | static Adapter regression 6/6 PASS; conforming rerun derived a compiler-owned non-caseable requirement gap and returned `need_user_answers` |
| A failed Case candidate copied unrelated positive Testability provenance into a formal Blocked root; the next clarification delivery then failed run-integrity preflight | classification now intersects failed-candidate evidence with the obligation source/oracle/routed-fact closure before deriving the root | dedicated stage regression 1/1 PASS; classification 33/33 PASS; classification/coverage/stage suites 115/115 PASS; clarification recovery completed through revisions 0–3 |
| Updating the bundled compiler left the checked-in public-pilot target comparator bound to the old digest | comparator binds the product implementation commit and current bundle; catalog binds the new comparator bytes; a checked-in-catalog regression makes future drift part of `npm run check` and verifies every declared revision blob through Git | regression RED `invalid`, then 1/1 PASS; `npm run public-pilot` returns `pilot_ready`, `release_eligible=false`, `release_status=insufficient_evidence`, `captures_ready=false` |

The classification change deliberately retains independent Testability claims in
accepted Evidence Claims and Case Drafts for audit, while preventing them from
being promoted into the formal requirement root unless they are in its formal
closure. Structured `unknown` or `unavailable` status still drives Blocked.

## Preliminary forward tests

These observations found and verified the G4 fixes, but are invalidated as final
evidence by the later production or benchmark-asset changes. Fresh final runs are
required from the frozen isolated candidate.

### Complete PRD

- Input SHA-256:
  `518f75a9c353e452169334625183fa0a3920d140f5cbcbfa6f7a6a32dd2432e1`.
- Conforming run directory:
  `/Users/zhangxudong/.codex/visualizations/2026/08/29/01a04b4c-854a-70f1-82b2-70bc209ea9f0/g4-complete-conforming-20260902-run`.
- The four accepted Agent artifacts contained no root IDs, obligation IDs, or
  fifth artifact. F040 lacked direct independent exclusion proof and became root
  `root_93789afa96fc8cfb` with compiler-owned gap
  `obligation_a37c58fb1e705536`, `caseable=false`.
- Reply stopped at `need_user_answers`; it did not launder the responsibility to
  NotApplicable or a Case.

### Incomplete module note

- Input SHA-256:
  `8776a4a8a00c4b3dfb7b35a05040e4181ba6ed4c06a7576b5f2ca987d877b5fe`.
- Run directory:
  `/private/tmp/generate-test-cases-forward.BUSxnh/agent-incomplete-run`.
- The runner accepted exactly the four writable artifacts and returned
  `need_user_answers` with ten compiler-owned roots; no bundle was published and
  missing product facts were not guessed.

### Clarification and durable recovery

- Input SHA-256:
  `b7290318bff4db7c6d789d511b8e523ce79b7f8e4749e7abb1ca899133b2bc73`.
- RED run:
  `/private/tmp/generate-test-cases-forward.BUSxnh/agent-clarification-run`.
  Revision 1 delivery failed with `RUN_INTEGRITY_ERROR` because unrelated
  Testability evidence had polluted the prior formal root.
- Fixed run:
  `/private/tmp/generate-test-cases-forward.BUSxnh/agent-clarification-fixed-run`.
  Revision 0 asked eight roots; revision 1 applied one `unknown`, one `deferred`,
  and exact delivery and finished; a no-change reinvocation did not re-ask;
  revision 2 explicitly reopened only one root; revision 3 stopped for
  `no_information_gain`; final reinvocation returned the same highest revision
  and digest `e454f5a80fb314b8082ba59630a974424946e7562d465156852652806e3b9c98`.
- The final preliminary bundle was 0 Grounded, 0 Conditional, 11 Blocked, which
  is the fail-closed result for the supplied evidence.

Three agent roles were used across these forward checks: complete-input test
expert, incomplete/recovery test expert, and independent spec/code-quality
reviewer. The platform thread limit allowed one fresh context and required reuse
of two existing contexts; no result is described as three fresh contexts.

## Preliminary engineering verification

After commit `c76fcee9ef984642226d9fdbd63a217c6de93439` and before the public-pilot
binding-only changes:

```text
npm run check
```

Exit 0:

- TypeScript no-emit PASS;
- generated bundle freshness PASS;
- primary suite 841/841 PASS in 405.120 s;
- repeatability 1/1 PASS: 100 fresh installed-shape runs in 46.744 s.

```text
/private/tmp/generate-test-cases-validator-py/bin/python \
  /Users/zhangxudong/.codex/skills/.system/skill-creator/scripts/quick_validate.py \
  skill/generate-test-cases
```

Exit 0: `Skill is valid!`.

```text
npm run benchmark
```

Exit 0 with `status=insufficient_evidence`. The synthetic fixture reported no
categorical hard failure and no unsupported critical/high Grounded Oracle, but it
does not contain the required real corpus, external expert labels, adjudication,
captures, denominators, slices, and confidence intervals.

```text
node --check skill/generate-test-cases/scripts/test-compiler.mjs
node build/build.mjs --check
git diff --check
```

All exited 0.

## Independent review

The blocker evidence-closure change received independent Spec and code-quality
APPROVE. Review confirmed that formal fact/oracle evidence is preserved,
independent Testability provenance remains auditable but is not promoted into a
formal root, legal revision delivery proceeds to `need_artifact/evidence_claims`,
and the existing invalid-delivery/reopen/Decision gates remain closed.

The public-pilot binding change received REQUEST CHANGES because its first
regression verified only declared digest against working-tree bytes. The revised
test now requires each frozen `repository_revision` to resolve to a commit and
recomputes every artifact digest from
`git show <revision>:<repository_path>` without requiring revision to equal HEAD.
Independent re-review then returned APPROVE. Any later production, Schema, Skill,
or benchmark-asset change invalidates the preliminary evidence and requires a new
freeze.

## Pre-freeze digests

- modular compiler source aggregate:
  `52b73e3268bb19418d8d864e2737c528a97f498a554ae5c4d0036920cc3ffaec`
- Schema aggregate:
  `5c8bdc6d6b2d3c22b3fcf44f6195d83f683a73c57cc9008b8ac746f7aeb3e91c`
- Schema manifest:
  `f706ff91b97ea1ee336f5abda96d3ff348804c71a5961db217bdddd971bb047a`
- `SKILL.md`:
  `06f974e581b710490cdb45a1cfc79e3014ff4326579b56618d2edcad87999a94`
- generated bundle:
  `6532625396569079b9e173885c1336c4ff53f409c1f97dc988daba80663344b8`
- formal benchmark manifest:
  `d7e4bd92ec8ece2359c706ddb58768dbae84f5ba1338f3c9788e7174c1922eff`

These are pre-freeze values. The final isolated run must recompute them from the
exact frozen candidate and must not reuse this section as release evidence.
