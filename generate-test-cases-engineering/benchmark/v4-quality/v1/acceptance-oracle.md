# Human acceptance oracle

This file is withheld from generation contexts. It defines semantic review targets without fixing Case counts, titles, or wording.

| Family | Required business meaning | Forbidden additions or losses |
| --- | --- | --- |
| F01 | Keep title-required and max-length outcomes; keep publish and visibility single points; form supported same-announcement create→publish visibility and create→publish→withdraw paths with their own terminal result. | Do not merge every rule into one Case, replace single points with one flow, switch announcement objects, or invent sorting/API behavior. |
| F02 | Preserve AND versus OR/exception semantics, independently diagnosable denial conditions, title unchanged on denial, and evidence-backed impossibility of simultaneous mutually exclusive states. | Do not run a Cartesian product, merge distinct restrictions because copy matches, or confuse prohibited action with missing execution data. |
| F03 | Ask for the missing time boundary before Cases; ordinary generation/continue does not authorize closure. Apply final/E1/unknown/defer/delivery/partial semantics only to the selected current item. | Do not guess hours, auto-defer, auto-deliver, or reuse a choice for a later root version. |
| F04 | Distinguish selection from query trigger; use matching and nonmatching data to detect false positives; separately cover 10→草稿 and 20→已发布 for the same record. | Do not assert total count/order/paging or let mocked mapping prove real filtering. |
| F05 | Distinguish A entered, A validation passed/failed/pending, and B availability. Keep required validation action in the Case when it is part of the target. | Do not hide validation success in preconditions or demand real organization IDs during generation. |
| F06 | UI-only uses only UI state. Interface contract distinguishes request emission from `accepted` business result and prepares transient observation before click. Missing success rule is a semantic question only when success is a target. | Do not require all surfaces, invent credentials/tools/fields/status codes, or equate HTTP 200 with business success. |
| F07 | Read the actual image and both comment pages; preserve thread ancestry and terminal page. Treat styling/copy as reference, reject C-101 auto-pin, adopt C-102 automatic visibility, and report unavailable/offline limits truthfully. | Do not claim pixel/copy acceptance, treat resolved/newest as authority, or claim an excerpt exhausts online content. |
| F08 | Preserve exact range and formula evidence, replaceable example nature, legal 80.00 derivation, and the non-normative overview. Missing formula/parameter needed for a target remains a question. | Do not use the overview as a detailed rule, guess missing inputs, or delete the target to avoid a gap. |

For every final result, review source→Case accounting, Case→exact support, and whether the target could be wrong while the chosen signal still appears normal. Open the actual HTML and inspect the actual Table; file existence and matching counts are insufficient.
