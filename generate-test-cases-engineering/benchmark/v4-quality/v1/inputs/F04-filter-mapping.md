# 记录查询与状态展示

用户选择记录类别并点击“查询”后，列表仅展示该类别的记录。只改变类别选择而不点击“查询”时，当前列表结果不改变。

验收数据中可以准备至少一条所选类别记录和至少一条其他类别记录。需求没有声明全库记录总数、默认排序或分页结果。

另有独立状态映射规则：同一条响应记录的 `state_code` 为 `10` 时，界面状态显示“草稿”；为 `20` 时，显示“已发布”。其他取值不在本次验收范围。

本需求没有声明模拟响应可以证明真实服务端筛选正确。
