# 记录更名规则（OR/例外版本）

记录只有“草稿”和“已发布”两种互斥状态，同一时刻只能处于其中一种状态。两种角色都可能访问两种状态的记录。

操作者提交非空新标题后，满足以下任一条件即可更名成功：

1. 操作者拥有“专门授权”；
2. 操作者具有普通操作权限，并且记录当前为草稿。

成功后，同一记录的标题变为提交值。不满足任一允许条件时拒绝更名，且标题保持不变。

本文件独立生效，不与 AND 版本同时使用。
