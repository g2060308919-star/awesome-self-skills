# 公告管理需求

编辑者可以创建公告草稿。创建时必须填写标题；标题最多 20 个中文字符。标题缺失或超过上限时拒绝保存。

保存草稿后，该公告出现在创建它的编辑者的公告列表中，读者列表中不可见。

编辑者可以发布自己创建的草稿。发布后，同一公告在读者列表中可见。

编辑者可以撤回自己发布的公告。撤回后，同一公告仍保留在该编辑者的列表中，状态恢复为“草稿”，并从读者列表移除。

本需求未声明其他角色权限、排序、分页、接口字段、提示文案或时间限制。
