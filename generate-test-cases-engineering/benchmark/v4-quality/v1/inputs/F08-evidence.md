# 折扣规则与依据性质

确定规则：订单原价必须大于 0 且不超过 10000 元。折后金额按“原价 × (1 - 折扣率)”计算，计算结果四舍五入保留两位小数。折扣率必须在 0 到 0.50 之间（含边界）。

可替换示例：可以用原价 100 元、折扣率 0.20 演示计算，但该示例不是唯一允许输入。

确定验收实例：原价 100 元、折扣率 0.20 时，折后金额为 80.00 元。

概述：该功能帮助商家灵活促销。此概述没有定义额外折扣范围、权限、时间窗口或提示文案。
