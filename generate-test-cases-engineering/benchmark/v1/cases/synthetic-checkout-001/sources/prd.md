# Synthetic pilot: checkout authorization

This is invented test-fixture material, not a real product PRD or release evidence.

When an authorized buyer submits a valid cart, the system creates an order in `pending-payment`. The source intentionally does not define the failed-payment final state or message.
