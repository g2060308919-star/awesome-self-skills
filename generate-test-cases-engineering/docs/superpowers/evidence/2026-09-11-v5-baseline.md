# Generate Test Cases V5 Baseline Evidence

## Authority and source tree

- Normative contract: the user-provided `03-development-spec.md`.
- Background only: the user-provided problem record and technical design.
- Authorized writable source: `generate-test-cases-engineering/`.
- Generated installation source: `generate-test-cases-engineering/skill/generate-test-cases/`.
- Published repository bundle: `generate-test-cases/`.
- Starting commit: `858fdd1de77ba31655ff57810fbf0ff8a47872c9`.
- Starting worktree: clean `main`, equal to `origin/main`.

The source tree contains modular compiler source, the build command, registered
Schemas, tests, and byte-identical installed/published bundle checks. It is not
an untraceable installed-only artifact, so `TARGET_SKILL_SOURCE_UNAVAILABLE`
does not apply.

## Frozen V4 interface

- Schema: `4.0.0`.
- Compiler: `0.5.0`.
- Public exports: `advanceStrict`, `constructV4Action`,
  `createV4RunDirectory`, `sourceAcquisitionMaterialPathV4`, and
  `stageV4SourceAcquisitionAction`.
- Registered Schemas: 16.
- Agent artifacts: `source_pack`, `evidence_claims`, `behavior_views`, and
  `case_drafts`.

These symbols are baseline evidence only. V5 cutover removes them from the
operational runtime instead of retaining a compatibility dispatch.

## Characterization results before modification

| Command | Result |
|---|---|
| `npm run check` | 1,447 serial core/golden/interface/recovery tests passed; 2 repeatability tests passed; 0 failed |
| `npm run test:benchmark` | 145 passed; 0 failed |
| `npm run public-pilot` | `pilot_ready`; 30/30 admitted; command exited 0 |
| `git diff --check` | passed |

The public pilot correctly reported `release_eligible=false` and
`release_status=insufficient_evidence`; the V4 README explicitly excludes the
separate single-system release benchmark from development acceptance. This is
not a hidden baseline failure.

## V5 replacement target

- Schema: `5.0.0`.
- Compiler: `0.6.0`.
- Public exports: `createV5RunDirectory`, `advanceV5Run`, and `inspectV5Run`.
- Requirements: C01–C16, each with positive and negative/Blocked fixture
  evidence discovered only through the V5 fixture manifest.
- Legacy policy: no V3/V4 operational reader, writer, generator, dispatch,
  fallback, migration, or version-selection path.

Machine-readable details are in `v5-symbol-schema-map.json`.
