# 检查点、探索与结果模型

## 权威标准

`expected[].text` 是唯一通过标准。页面显示近义词不能自行放宽；例如期望“评分”而实际只有“用户评分”，若精确语义不满足就记录未通过和实际文字。运行后的新标准只能进入新用例和新 Run。

每个 oracle 映射为 `<case_id>/<step_id>/<oracle_id>`，独立保存事实与证据状态。不得因后续阻塞覆盖已完成项。

## 固定四态

| 内部值 | 展示 | 使用条件 |
|---|---|---|
| `passed` | 通过 | 明确事实支持满足原文 |
| `failed` | 未通过 | 明确事实支持不满足原文 |
| `undetermined` | 无法确定 | 已尝试但事实不足 |
| `not_executed` | 未执行 | 整条用例在正式执行前明确排除 |

缺账号、环境/工具阻塞、没有动态样本或执行中断均不是 `not_executed`，而是 `undetermined`。用户明确无法提供某个角色但至少一个其他角色可执行时，缺失角色相关检查点为 `undetermined`；不得记为产品 `failed` 或预先排除的 `not_executed`。

聚合顺序固定：任一检查点 failed → case failed；否则任一必需检查点 undetermined → undetermined；否则全部必需检查点 passed → passed；否则若整条 case 预先排除 → not_executed；其余 → undetermined。证据、截图、代理、清理和 Run 状态独立，不能改写产品结果。

原因必须是语义化中文事实，说明期望、实际、位置和影响；不能只写错误码、`false`、`timeout` 或“与预期不符”。通过也要写观察事实。

## 报告

报告由快照与日志生成，包含 Run/时间/目标/角色、四态计数、固定总表、未通过、无法确定、协助/阻塞、证据/数据处理、代理验证、清理和一致性。

总表表头精确为：

```text
ID | 模块 | 测试场景 | 测试结果 | 成功/失败的原因
```

每条输入用例按原始顺序恰好出现一次，使用原始 case ID 和四种中文结果。不得增加原因分类列。截图失败只能写在证据状态/原因中，不能改变已有产品判断。

## 对话交付

最终对话中的四态统计和五列表格必须直接复制自已校验的同一份 `report.md`，不得依据记忆重新汇总或判定。对话表头、ID、模块、场景、结果、原因和顺序与报告完全一致；每条输入用例恰好一行，即使用例很多也不能省略通过项。报告链接不能替代对话内全量表格。报告生成或一致性校验失败时，不得宣称 Run 完成或拼造表格。

最终顺序为：Run 根目录和四态数量；完整五列表格；未通过和无法确定重点；代理验证与自动清理结果（未使用时写“本次未使用代理”）；`report.md` 链接和一致性结果。密码及其他认证秘密不得出现在最终回复。
