interface:
  display_name: "B2B E2E Runner"
  short_description: "Run confirmed B2B E2E cases with staged permissions and auditable reports"
  default_prompt: "Use $b2b-e2e-runner to execute these confirmed semantic cases in permission-ready batches through Chrome DevTools MCP, preserve real critical screenshots or explicit evidence gaps, and produce one auditable ledger plus the validated HTML report and complete dialogue table."
